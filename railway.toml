[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python rdbear_notouch_explorer.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
# No healthcheckPath: this is a background worker, not an HTTP service.
